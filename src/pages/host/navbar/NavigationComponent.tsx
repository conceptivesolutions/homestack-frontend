import React from "react";
import "./NavigationComponent.scss";

export default ({className}: { className?: string }) => (
  <div className={(className || "") + " navigation__container"}>

  </div>
)
